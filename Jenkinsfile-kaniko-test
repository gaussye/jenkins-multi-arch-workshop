  pipeline {
    agent none
    stages {
      stage('Build with Maven'){
        agent {
             kubernetes {
               yamlFile 'Jenkins-maven.yaml'
             }
        }
        steps {
          container('maven') {
             sh 'mvn clean package -Dmaven.test.skip=true'
          }
        }
      }
    }
}
